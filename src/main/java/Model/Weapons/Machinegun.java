package Model.Weapons;

import Model.Weapon;
import Model.WeaponType;

public class Machinegun extends Weapon {
    public Machinegun(WeaponType type, int damage) {
        super(type, damage);
    }
}